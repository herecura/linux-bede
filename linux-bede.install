# arg 1:  the new package version
# arg 2:  the old package version

vmlinuz_naming_warning() {
    echo "With linux-bede >= 5.3.9 we will use the move functionality provided in mkinitcpio 27+"
    echo "this change however will change the kernel filename from:"
    echo "/boot/vmlinuz-bede"
    echo "TO:"
    echo "/boot/vmlinuz-linux-bede"
    echo "You will have to update your bootloader configuration with these changes"
}

#post_install () {
    #vmlinuz_naming_warning
#}

post_upgrade() {
    vmlinuz_naming_warning
}
